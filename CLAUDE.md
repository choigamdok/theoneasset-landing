# 이 저장소에서 작업할 때

더원자산관리 랜딩 페이지. 빌드 도구도 의존 패키지도 없는 **정적 HTML**이다.
`npm install` 할 것도, 실행할 서버도 없다. 브라우저로 파일을 그냥 열면 된다.

## 파일

```
index.html            메인 랜딩 — 더원자산관리 소개·상담 유도
jung-yong-man.html    정용만 개인 페이지 (AI INDB System Architect)
assets/               이미지
```

두 페이지는 **서로 독립돼 있다.** 공용 CSS 파일이 없고 각 파일 안에 `<style>` 로
들어 있으므로, 한쪽 디자인을 고쳐도 다른 쪽은 따라오지 않는다. 양쪽을 맞춰야 하는
변경이면 두 파일을 다 고친다.

## 반드시 지킬 것

**1. 실적 수치를 임의로 바꾸지 않는다**

페이지에 이런 문구가 들어 있다.

- `1,000억` (운용 자산)
- `1,000명+` (고객 수)

이건 회사의 대외 홍보 문구다. 카피를 다듬는 과정에서 숫자를 반올림하거나 "약",
"이상" 같은 말을 붙이거나 빼지 않는다. 수치 변경은 **반드시 사용자 확인을 받는다.**
문구를 지어내지도 않는다.

**2. 시작할 때 `git pull`, 끝나면 push**

맥에어·맥미니 두 대에서 작업한다. 시작 전에 받아오고, 끝나면 올린다.
충돌이 나면 임의로 진행하지 말고 사용자에게 알린다.

**3. 수동 배포 금지**

`vercel --prod` 같은 CLI 직접 배포를 쓰지 않는다. 로컬 코드가 다른 기기 작업을
덮어쓴다. push만 한다.

**4. main push 전에 승인받는다**

main에 올라가면 그대로 공개된다. 사용자 승인 후에 push한다.

## 확인

서버가 필요 없다. 파일을 브라우저로 열어서 본다.

```bash
open index.html
open jung-yong-man.html
```

**모바일 폭에서 반드시 확인한다.** 고객이 카톡 링크로 들어오는 페이지라 대부분
휴대폰으로 본다. 브라우저 개발자도구에서 폭을 390px 정도로 좁혀 깨지는 곳이 없는지
본다. 데스크톱만 보고 끝내지 않는다.

## 외부 의존

인터넷에서 폰트를 가져온다. 오프라인에서는 폰트가 다르게 보일 수 있으나 정상이다.

- Pretendard (jsDelivr)
- Noto Serif KR (Google Fonts)

## 배포

Vercel 프로젝트 이름은 저장소 이름과 다르다.

| | 이름 |
|---|---|
| 저장소 | `theoneasset-landing` |
| Vercel 프로젝트 | `landing-page` |

찾을 때 헷갈리기 쉬우니 주의한다. 새 Vercel 프로젝트를 만들지 않는다.

회사 공식 사이트(`https://theoneasset.com`)로 나가는 링크가 있다. 이 저장소가
그 사이트는 아니다.
